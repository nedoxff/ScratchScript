@useFloatEquation(0.01)
import 'scratch/looks';
import 'scratch/operators';

function d(x: number, y: number)
{
    return x + y;
}

on start {
    for(let i = d(1, 1); i < d(5, 5); i = d(i, 1)) {
        sayTimed(toString(i), 1);
    }
}