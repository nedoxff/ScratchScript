import "scratch/looks";
import "scratch/operators";

function plus(@num x, @num y) {
    return x + y;
}

on start {
    for(var i = 2; i < 1000; i = plus(i << 2 ** 3, 5)) {
        sayTimed(i.toString(), 1);
    }
    
    for(var i = 0;;i += 1) {
        say(i);
    }
}